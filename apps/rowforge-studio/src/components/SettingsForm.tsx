/**
 * Plan 6 T10 scaffold — T11 implements the actual controlled form.
 * Renders nothing meaningful for now so /settings can mount.
 */
export function SettingsForm() {
  return (
    <div className="text-muted-foreground">
      Settings form coming in T11.
    </div>
  );
}
