export function RowHistoryDrawer({
  executionId: _executionId,
  seq: _seq,
  onClose: _onClose,
}: {
  executionId: string;
  seq: number | null;
  onClose: () => void;
}) {
  return null; // Task 18 fills in.
}
