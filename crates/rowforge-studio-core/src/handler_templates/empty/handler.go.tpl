package main

// {{name}} — empty handler scaffold. Replace this with your row-handling logic.
//
// The rowforge protocol expects this binary to:
//   1. Read an "init" envelope from stdin (JSON, single line, ends with \n)
//   2. Write a "ready" envelope to stdout
//   3. Loop: read row envelopes from stdin, write outcome envelopes to stdout
//   4. Exit cleanly on EOF or SIGTERM
//
// See examples/handlers/ in the rowforge repo for working templates.

func main() {
}
